import React from "react";
import {Page, Text, Image, Document, Stylesheet} from 'react-pdf/renderer';
import Logo from "../HighlightedText/logo.svg";

const styles = Stylesheet.create({});

const PDFCreate = () => {
    return(
        <div>test</div>
    )
}

export default PDFCreate;